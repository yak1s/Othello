package app.tidemark.ui.bulk

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Shown by the watches screen while rows are multi-selected (long-press). Actions: How often,
 * Collection, Pause, Resume, Check now, Stop. Owned by the ui-app package.
 */
@Composable
fun BulkEditBar(selectedIds: Set<Long>, onClearSelection: () -> Unit, modifier: Modifier = Modifier): Unit =
    TODO("ui-app package")
