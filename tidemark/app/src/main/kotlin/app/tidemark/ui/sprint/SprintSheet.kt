package app.tidemark.ui.sprint

import androidx.compose.runtime.Composable

/**
 * Start (or stop) a Sprint for one watch: 20-60 s checks for 20 min (default), 30 min, 1 h or 2 h,
 * ending automatically. Owned by the ui-app package.
 */
@Composable
fun SprintSheet(watchId: Long, onDismiss: () -> Unit): Unit = TODO("ui-app package")
