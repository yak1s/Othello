package app.tidemark.ui.watches

import androidx.compose.foundation.lazy.LazyListState
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Home: the Now strip, the dense list on the numeric rail, the top pill (search, filters, ticker).
 * [listState] is hoisted so the scaffold can hide the bottom pill on scroll down.
 * Owned by the ui-screens package.
 */
@Composable
fun WatchesScreen(listState: LazyListState, modifier: Modifier = Modifier): Unit = TODO("ui-screens package")
