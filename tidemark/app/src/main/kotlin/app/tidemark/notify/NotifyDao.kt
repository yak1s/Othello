package app.tidemark.notify

import androidx.room.Dao

/**
 * Extra queries owned by the notify package (A4). Add any query this package needs here
 * (the shared DAOs in data/db/Daos.kt are frozen). Registered in TidemarkDatabase.
 */
@Dao
interface NotifyDao
