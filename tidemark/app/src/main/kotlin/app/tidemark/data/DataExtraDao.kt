package app.tidemark.data

import androidx.room.Dao

/**
 * Extra queries owned by the data package (A1). Add any query this package needs here
 * (the shared DAOs in data/db/Daos.kt are frozen). Registered in TidemarkDatabase.
 */
@Dao
interface DataExtraDao
