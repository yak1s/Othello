package app.tidemark.widget

import androidx.room.Dao

/**
 * Extra queries owned by the widget package (A5). Add any query this package needs here
 * (the shared DAOs in data/db/Daos.kt are frozen). Registered in TidemarkDatabase.
 */
@Dao
interface WidgetQueriesDao
